
## 2026-06-08 20:36:57 音视频推流优化

- audio_streamer.py：移除未使用的 struct，引入客户端 busy 状态、发送完成回调、慢客户端丢包策略、无客户端/缓冲过长时丢弃旧音频。
- stream_manager.py：替换网页端 PCM 播放逻辑，使用 audioQueue、nextAudioTime 和 AudioContext.currentTime 进行连续排队播放，避免 Float32Array 大量复制。
- config.py：STREAM_SKIP_FRAMES 1 -> 2，STATUS_SKIP_FRAMES 1 -> 5，STREAM_JPEG_QUALITY 55 -> 45，AUDIO_STREAM_CHUNK_MS 50 -> 100。

## 2026-06-08 20:38:24 复查补丁

- audio_streamer.py：补充 stop() 时清空 _client_busy；发送完成回调中避免断开客户端留下陈旧 busy 项。


## 2026-06-08 20:47:04 日志问题修复

- audio_streamer.py：handle_client(websocket, path) 改为 handle_client(websocket, path=None)，兼容新版 websockets 单参数 handler。
- recorder_manager.py：音频录制初始化失败时不再清理并返回 False，而是保留视频编码器继续录制视频，音频路径置空并打印降级提示。


## 2026-06-08 20:53:06 音频无声补丁

- audio_streamer.py：广播前将 pcm_data 强制转换为 bytes；转换失败时打印 [音频推流] PCM 转换失败。
- audio_streamer.py：发送 future 异常时打印 [音频推流] 发送失败，移除客户端，避免静默断线。


## 2026-06-08 21:01:50 浏览器播放与采集诊断增强

- stream_manager.py：Web 音频播放改为 createScriptProcessor 的 onaudioprocess 拉取队列输出，加入采样率重采样、3倍增益和页面峰值显示。
- audio_streamer.py：新增 _calc_pcm_peak，每 30 个音频块打印 len/peak/dropped，判断麦克风是否采到有效 PCM。


## 2026-06-08 21:15:14 音频发送循环修复

- audio_streamer.py：新增 _drain_audio_buffer(target_frames, max_reads)，连续清理旧音频缓冲。
- audio_streamer.py：客户端连接后缓冲过长不再直接 continue，而是清理后继续读取并广播最新 PCM。
- audio_streamer.py：发送统计日志从每 30 块改为每 5 块。


## 2026-06-08 21:19:53 版本标记更新

- audio_streamer.py：版本日志从 audio-debug-20260608-2105 更新为 audio-debug-20260608-2118-drain-fix。
- audio_streamer.py：补充方法间空行，便于代码核对。


## 2026-06-08 21:24:41 音频直接读取补丁

- audio_streamer.py：移除 get_remaining_frames() 硬阈值等待，有客户端时直接 record(self._chunk_ms)。
- audio_streamer.py：新增 _empty_reads 计数和 [音频推流] 空读日志。
- audio_streamer.py：版本标记更新为 audio-debug-20260608-2128-direct-record。


## 2026-06-08 21:30:47 平滑音频补丁

- audio_streamer.py：补充 self._empty_reads 初始化，消除采集循环异常。
- audio_streamer.py：新增 self._client_pending，替代一忙就丢包的策略，最多允许 3 个在途发送包。
- audio_streamer.py：版本标记更新为 audio-debug-20260608-2138-smooth-audio。


## 2026-06-08 21:34:31 阻塞录音器补丁

- audio_streamer.py：audio.Recorder(..., block=False) 改为 block=True。
- audio_streamer.py：采集循环移除 remaining 阈值逻辑，直接阻塞读取 record(self._chunk_ms)。
- audio_streamer.py：版本标记更新为 audio-debug-20260608-2148-blocking-recorder。


## 2026-06-08 21:38:21 降低音频丢包补丁

- config.py：AUDIO_STREAM_CHUNK_MS 100 -> 200。
- audio_streamer.py：pending 丢包阈值 3 -> 10；发送日志改为每 10 块打印；版本标记更新为 audio-debug-20260608-2158-low-drop。
- stream_manager.py：AUDIO_GAIN 3.0 -> 1.5；MAX_AUDIO_QUEUE_SECONDS 0.8 -> 1.5；网页版本标记同步为 audio-debug-20260608-2158-low-drop。


## 2026-06-08 21:42:02 音质平滑调参

- config.py：AUDIO_STREAM_CHUNK_MS 200 -> 250；STREAM_SKIP_FRAMES 1 -> 2。
- stream_manager.py：AUDIO_GAIN 1.5 -> 1.0；MAX_AUDIO_QUEUE_SECONDS 1.5 -> 2.2；新增高通、噪声门和软限幅。
- audio_streamer.py / stream_manager.py：版本标记更新为 audio-debug-20260608-2210-smoother-noisegate。


## 2026-06-08 21:47:23 稳定帧率调参

- config.py：AUDIO_STREAM_CHUNK_MS 250 -> 300，STREAM_SKIP_FRAMES 保持 2。
- audio_streamer.py：pending 阈值 10 -> 3；发送日志每 40 块打印；版本标记更新为 audio-debug-20260608-2225-stable-fps。
- stream_manager.py：MAX_AUDIO_QUEUE_SECONDS 2.2 -> 2.8；保留 AUDIO_GAIN=1.0、高通、噪声门和软限幅。


## 2026-06-08 21:54:14 音频开启时自适应视频降载

- audio_streamer.py：无客户端时不再调用 _drain_audio_buffer/record，避免占用麦克风和多媒体资源；版本标记更新为 audio-debug-20260608-2238-adaptive-video。
- main.py：每轮读取 audio_client_count；音频客户端存在时 status_skip 至少为 10，stream_skip 至少为 4。


## 2026-06-08 21:57:53 关闭实时音频采集

- config.py：AUDIO_STREAM_ENABLE True -> False，取消网页实时麦克风音频推流。
- 其他视频推流、人脸识别、状态服务和现有代码结构保持不变。


## 2026-06-08 22:10:08 撤销音频触发视频降频

- main.py：删除 audio_client_count>0 时 stream_skip=max(stream_skip,4) 的逻辑，避免点击音频后网页视频帧率突降。
- 验证：python -m py_compile main.py audio_streamer.py stream_manager.py config.py 通过。


## 2026-06-08 22:28:00 低占用语音识别模型接入

- config.py：VOICE_ENABLE False -> True；新增 VOICE_PAUSE_WHEN_ACTIVE、VOICE_RUN_INTERVAL_MS、VOICE_PAUSE_SLEEP_MS、VOICE_KWS_GATE。
- voice_recognition.py：新增暂停回调、KWS 阈值参数和固定轮询间隔，避免 10ms 紧轮询长期抢占资源。
- main.py：初始化 VoiceRecognition 时传入低占用参数；人脸识别、录制、录入状态暂停语音识别，空闲状态保留关键词入口。


## 2026-06-08 22:36:00 语音模型默认关闭以避免内存崩溃

- config.py：VOICE_ENABLE True -> False，避免人脸模型常驻后再加载 nn.Speech 触发 Memory not enought 退出。
- config.py：补充注释说明当前硬件资源限制；低占用语音识别代码保留，默认走串口语音命令。


## 2026-06-08 22:48:00 设置页推流开关与 FPS 显示清理

- ui_pages.py：删除 HomePage 顶部 FPS 显示。
- main.py：删除 AppState.fps、FPS 计数变量和 _update_fps 调用。
- ui_pages.py：设置页新增 RTSP 推流开关，并调整音频/LED/按钮纵向位置。
- main.py：新增 _sync_stream_settings，设置页 HTTP/RTSP 开关会实际启动或停止对应推流服务。
- stream_manager.py：RtspStreamManager 新增 stop()，底层支持 stop 时调用，否则清理引用并更新状态。


## 2026-06-09 WAV 文件落盘与显示修复

- recorder_manager.py：_sync_audio 不再依赖 get_remaining_frames，改为 50ms 周期直接 record()。
- recorder_manager.py：新增音频块/空读计数日志；停止录制时检查 WAV 是否真实存在且有有效数据。
- main.py：录像列表扫描同名 WAV，并在清空录像时同步删除关联 WAV。
- ui_pages.py：录像管理列表显示 `+WAV` 或 `无音频`。

## 2026-06-09 带音频视频合成与播放

- video_player_manager.py：新增 VideoPlayerManager，负责同名 MP4/WAV 合成为 `_av.mp4`，并使用 MaixPy `video.Decoder` + `audio.Player` 播放。
- main.py：初始化 VideoPlayerManager；录制退出时自动尝试合成带音频视频；录像页播放按钮播放最新录像，缺少 `_av.mp4` 时先补合成。（后续已按用户要求撤销自动合成和录像页播放入口，见下方独立页面调整。）
- main.py：录像列表跳过 `_av.mp4` 独立条目，将 `av_name/av_size` 作为原录像的附属信息；清空录像时同步删除 MP4、WAV、`_av.mp4`。
- ui_pages.py：录像管理页新增“播放”按钮，列表标记 `+AV`、`+WAV` 或 `无音频`。
- app.yaml：加入 `video_player_manager.py`，确保打包上传到设备。
- 验证：`python -m py_compile main.py ui_pages.py video_player_manager.py` 通过。

## 2026-06-09 独立融合播放页面调整

- video_player_manager.py：新增 `list_video_items()`、`count_muxed()`、`mux_all()`，由播放器管理器独立负责扫描、统计和批量融合。
- ui_pages.py：主页新增“融合”按钮；新增 `FusionPlayerPage`，提供“读取 / 融合 / 播放 / 返回”按钮、已融合数量显示、列表点击选中播放。
- ui_pages.py：撤销录像管理页的“播放”按钮，录像页只保留录像文件管理和 `+AV/+WAV/无音频` 状态显示。
- main.py：新增融合播放页实例和回调；进入融合页前停止识别/录制状态并刷新列表；撤销 `_on_exit_recording()` 自动融合。
- 验证：`python -m py_compile main.py ui_pages.py video_player_manager.py` 通过。


## 2026-06-09 音视频同步与录制卡顿修复

- config.py：RECORD_VIDEO_FPS 25 -> 10，降低录制文件目标帧率以匹配人脸识别+编码负载。
- recorder_manager.py：音频录制改为独立线程 block=True 读取，避免音频时长跟随视频帧率变短。
- recorder_manager.py：video.Encoder 优先传入 framerate，固件不支持时回退旧构造方式。
- recorder_manager.py：encode_frame 增加目标 FPS 节流，并在停止时打印视频帧数/跳过帧数。
- main.py：录制状态下每轮都尝试编码当前画面，避免短暂无人脸时视频缺帧。


## 2026-06-09 识别状态推理降频

- config.py：新增 RECOGNIZE_DETECT_INTERVAL_MS=250、RECORD_RECOGNIZE_INTERVAL_MS=500。
- main.py：识别等待状态不再每帧检测人脸，改为按间隔调用 detect_faces_only。
- main.py：录制状态不再每帧完整识别，改为按间隔 detect_and_recognize，中间帧复用缓存人脸结果。

