
## 2026-06-08 20:36:57 音视频网页推流修复计划

- [x] 定位视频 1fps 和音频无声/卡顿的资源竞争路径。
- [x] 优化设备端音频发送背压。
- [x] 优化网页端音频播放队列。
- [x] 调整默认推流配置降低主循环压力。
- [ ] 上板运行验证实际帧率和音频连续性。

## 2026-06-08 20:47:04 音频日志修复计划

- [x] 修复 websockets handler 签名不兼容。
- [x] 修复网页音频推流与本地 WAV 录制抢占麦克风导致自动录制失败。
- [ ] 上板验证网页点击音频后是否出现客户端连接日志，以及自动录制是否继续生成视频。


## 2026-06-08 21:57:53 音频采集取消

- [x] 关闭 AUDIO_STREAM_ENABLE，停止启动实时音频采集线程。
- [x] 保留其他功能不变。


## 2026-06-08 22:28:00 接入语音识别模型并保护帧率

- [x] 接入本机 nn.Speech 关键词识别模型入口。
- [x] 增加低占用轮询参数和关键词阈值。
- [x] 在识别/录制/录入高负载状态暂停语音识别，优先保证视频帧率。
- [x] 根据上板日志确认当前内存不足以同时运行人脸模型和 nn.Speech。
- [x] 默认关闭本机语音模型，保留串口语音命令以保证帧率和稳定性。


## 2026-06-08 22:48:00 设置页推流控制

- [x] 删除主页 FPS 显示。
- [x] 删除主循环 FPS 统计维护。
- [x] 设置页加入 HTTP 推流开关的实际服务同步。
- [x] 设置页加入 RTSP 推流开关。
- [x] 通过 py_compile 验证 main.py、ui_pages.py、stream_manager.py、config.py。
- [ ] 上板验证 HTTP/RTSP 开关启停日志和网页访问状态。


## 2026-06-09 WAV 录制文件排查

- [x] 定位 WAV 日志有路径但文件可能未落盘的原因。
- [x] 去掉 get_remaining_frames 写入门槛，改为周期直接读取音频。
- [x] 停止录制时验证 WAV 文件大小并打印有效性。
- [x] 录像管理页显示 MP4 是否有关联 WAV。
- [ ] 上板录制一次，确认日志出现音频块数和录像页显示 `+WAV`。

## 2026-06-09 带音频视频播放器

- [x] 新增 MP4 + WAV 合成管理器。
- [x] 曾接入录制后自动生成 `_av.mp4`，后续已按用户要求撤销。
- [x] 曾在录像管理页新增播放入口，后续已按用户要求撤销。
- [x] 播放时优先使用 `_av.mp4`。
- [x] 编译检查 main.py、ui_pages.py、video_player_manager.py。
- [ ] 上板确认设备系统包含 ffmpeg，并确认 `_av.mp4` 可正常带音频播放。

## 2026-06-09 独立融合播放页面

- [x] 主页新增“融合”触摸入口。
- [x] 独立页面支持读取录像与已融合视频列表。
- [x] 独立页面支持手动批量融合。
- [x] 页面显示已融合视频数量。
- [x] 页面支持点击列表选择融合视频并播放。
- [x] 撤销录制结束自动融合，减少对原录制流程的影响。
- [ ] 上板测试融合页面的性能占用和播放流畅度。


## 2026-06-09 音视频录制同步

- [x] 定位音频只有 5s 的原因：音频读取跟随低频视频/识别循环。
- [x] 将 WAV 写入移到独立阻塞音频线程。
- [x] 将录制目标 FPS 调整为 10。
- [x] 录制状态下短暂无人脸也继续编码当前画面。
- [x] py_compile 验证 recorder_manager.py、main.py、config.py。
- [ ] 上板验证 15s 录制的 WAV 时长是否接近 15s，MP4 卡顿是否改善。


## 2026-06-09 识别帧率优化

- [x] 定位点击识别后卡顿来自每帧人脸模型推理。
- [x] 识别等待阶段改为 250ms 检测一次。
- [x] 录制阶段改为 500ms 完整识别一次，中间帧复用识别结果。
- [x] py_compile 验证 main.py、config.py、recorder_manager.py。
- [ ] 上板验证点击识别后的屏幕帧率和录制流畅度。

